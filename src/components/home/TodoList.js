import React from "react";

function TodoList() {
  return (
    <div
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        height: "calc(100vh - 160px)",
      }}
    >
      TodoList
    </div>
  );
}

export default TodoList;
